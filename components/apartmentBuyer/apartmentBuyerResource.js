const { buyerConstants } = require('../buyer')

const brief = (buyer) => {
  return (({
    id,
    name,
    phone,
    authorID,
    responsibleID,
    buyerStatus,
    contract,
    motivation,
    source,
    reasonToBuy,
    description,
    createdAt,
    updatedAt,
    noOfRooms,
    districtID,
    maxPrice,
    material,
    buildingType,
    floor,
    authorName
  }) => ({
    id,
    name,
    phone,
    authorID,
    responsibleID,
    buyerStatus,
    contract,
    motivation,
    source,
    sourceName: buyerConstants.SOURCES[parseInt(source)],
    reasonToBuy,
    description,
    createdAt,
    updatedAt,
    noOfRooms,
    districtID,
    maxPrice,
    material,
    buildingType,
    floor: buyerConstants.APT_FLOORS[parseInt(floor)],
    authorName
  }))(buyer)
}

const full = (buyer) => {
  return (({
    id,
    name,
    phone,
    birthday,
    isOnViber,
    isOnTelegram,
    isOnFacebook,
    isOnWhatsapp,
    authorID,
    responsibleID,
    buyerStatus,
    contract,
    motivation,
    source,
    reasonToBuy,
    description,
    createdAt,
    updatedAt,
    noOfRooms,
    districtID,
    maxPrice,
    material,
    buildingType,
    floor,
    isRenovated,
    autonomousHeat,
    authorName
  }) => ({
    id,
    name,
    phone,
    birthday,
    isOnViber,
    isOnTelegram,
    isOnFacebook,
    isOnWhatsapp,
    authorID,
    responsibleID,
    buyerStatus,
    contract,
    motivation,
    source,
    sourceName: buyerConstants.SOURCES[parseInt(source)],
    reasonToBuy,
    description,
    createdAt,
    updatedAt,
    noOfRooms,
    districtID,
    maxPrice,
    material,
    buildingType,
    floor: buyerConstants.APT_FLOORS[parseInt(floor)],
    isRenovated,
    autonomousHeat,
    authorName
  }))(buyer)
}

module.exports = { brief, full }
